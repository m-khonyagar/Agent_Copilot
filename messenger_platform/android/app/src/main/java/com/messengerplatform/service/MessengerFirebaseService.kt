package com.messengerplatform.service

/**
 * Placeholder for Firebase Cloud Messaging integration.
 *
 * To enable push notifications via FCM:
 * 1. Add google-services.json to the app/ directory.
 * 2. Add the Firebase BOM and FCM dependency to build.gradle.kts.
 * 3. Implement onMessageReceived() to handle incoming push payloads.
 *
 * Without FCM, the app uses polling (NotificationListenerService) instead.
 */
class MessengerFirebaseService
